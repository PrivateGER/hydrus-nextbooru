/**
 * Centralized pagination constants.
 *
 * Keeping these in one place makes it easy to adjust page sizes
 * globally and ensures consistency across different views.
 */

/** Number of posts displayed per page in grid views */
export const POSTS_PER_PAGE = 48;

/** Number of tags displayed per page in the tags browser */
export const TAGS_PER_PAGE = 100;
